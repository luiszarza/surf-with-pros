<%= simple_form_for [@listing, @booking] do |f| %>
  <%= f.input :date %>
  <%= f.submit "Create Booking", class: "btn btn-primary text-white" %>
<% end %>
<br>
<%= link_to "Go back to listing", listings_path, class: "btn btn-primary text-white" %>
